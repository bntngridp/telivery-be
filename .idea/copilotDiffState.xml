<?xml version="1.0" encoding="UTF-8"?>
<project version="4">
  <component name="CopilotDiffPersistence">
    <option name="pendingDiffs">
      <map>
        <entry key="$PROJECT_DIR$/src/modules/auth/auth.routes.ts">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/src/modules/auth/auth.routes.ts" />
              <option name="originalContent" value="import { Router } from &quot;express&quot;;&#10;import {&#10;  registerBuyer,&#10;  loginOtp,&#10;  verifyOtp,&#10;  requestSellerOtp,&#10;  verifyOtpAndRegisterSeller,&#10;  registerSellerStore&#10;} from &quot;./auth.controller&quot;;&#10;import { uploadSellerDocs } from &quot;./multer.seller&quot;;&#10;&#10;const router = Router();&#10;&#10;router.post(&quot;/register/buyer&quot;, registerBuyer);&#10;router.post(&quot;/login-otp&quot;, loginOtp);&#10;router.post(&quot;/verify-otp&quot;, verifyOtp);&#10;router.post(&quot;/seller/request-otp&quot;, requestSellerOtp);&#10;router.post(&quot;/seller/register-account&quot;, verifyOtpAndRegisterSeller);&#10;&#10;router.post(&#10;    &quot;/seller/register-store&quot;,&#10;    jwtMiddleware,&#10;    uploadSellerDocs.fields([&#10;        { name: &quot;ownerKtpPhoto&quot;, maxCount: 1 },&#10;        { name: &quot;ownerFacePhoto&quot;, maxCount: 1 },&#10;    ]),&#10;    registerSellerStore&#10;);&#10;&#10;export default router;&#10;" />
              <option name="updatedContent" value="import { Router } from &quot;express&quot;;&#10;import {&#10;  registerBuyer,&#10;  loginOtp,&#10;  verifyOtp,&#10;  requestSellerOtp,&#10;  verifyOtpAndRegisterSeller,&#10;  registerSellerStore&#10;} from &quot;./auth.controller&quot;;&#10;import { uploadSellerDocs } from &quot;./multer.seller&quot;;&#10;import { jwtMiddleware } from &quot;./jwt.middleware&quot;;&#10;&#10;const router = Router();&#10;&#10;router.post(&quot;/register/buyer&quot;, registerBuyer);&#10;router.post(&quot;/login-otp&quot;, loginOtp);&#10;router.post(&quot;/verify-otp&quot;, verifyOtp);&#10;router.post(&quot;/seller/request-otp&quot;, requestSellerOtp);&#10;router.post(&quot;/seller/register-account&quot;, verifyOtpAndRegisterSeller);&#10;&#10;router.post(&#10;    &quot;/seller/register-store&quot;,&#10;    jwtMiddleware,&#10;    uploadSellerDocs.fields([&#10;        { name: &quot;ownerKtpPhoto&quot;, maxCount: 1 },&#10;        { name: &quot;ownerFacePhoto&quot;, maxCount: 1 },&#10;    ]),&#10;    registerSellerStore&#10;);&#10;&#10;export default router;" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/src/modules/auth/auth.service.ts">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/src/modules/auth/auth.service.ts" />
              <option name="originalContent" value="import { PrismaClient } from &quot;@prisma/client&quot;;&#10;import { RegisterBuyerDto, LoginOtpDto, VerifyOtpDto, RequestSellerOtpDto, VerifyOtpAndRegisterSellerDto, RegisterSellerStoreDto } from &quot;./auth.schema&quot;;&#10;import jwt from &quot;jsonwebtoken&quot;;&#10;import bcrypt from &quot;bcryptjs&quot;;&#10;&#10;const prisma = new PrismaClient();&#10;const OTP_EXPIRE_MINUTES = 5;&#10;const JWT_SECRET = process.env.JWT_SECRET || &quot;secret&quot;;&#10;&#10;export const authService = {&#10;    async registerBuyer(data: RegisterBuyerDto) {&#10;        const existing = await prisma.pembeli.findFirst({&#10;            where: {&#10;                OR: [&#10;                    { phone_number: data.phoneNumber },&#10;                    { email: data.email }&#10;                ]&#10;            }&#10;        });&#10;&#10;&#10;        if (existing) {&#10;            throw new Error(&quot;Nomor atau email sudah terdaftar.&quot;);&#10;        }&#10;&#10;        // Simpan data pembeli baru&#10;        const newBuyer = await prisma.pembeli.create({&#10;            data: {&#10;                full_name: data.fullName,&#10;                phone_number: data.phoneNumber,&#10;                delivery_address: data.address,&#10;                birth_date: new Date(data.birthDate),&#10;                domicile: data.domicile,&#10;                faculty: data.faculty,&#10;                major: data.major,&#10;                email: data.email,&#10;                // password: ... (jika ada)&#10;            }&#10;        });&#10;        // Generate OTP&#10;        const otp = Math.floor(10000 + Math.random() * 90000).toString();&#10;        const expiredAt = new Date(Date.now() + OTP_EXPIRE_MINUTES * 60000);&#10;        await prisma.otp_verify.create({&#10;            data: {&#10;                phone: data.phoneNumber,&#10;                otp,&#10;                expiredAt,&#10;            }&#10;        });&#10;        // Simulasi kirim OTP via console&#10;        console.log(`OTP ${otp} dikirim ke ${data.phoneNumber}`);&#10;        return {&#10;            message: &quot;Register berhasil. OTP dikirim ke nomor kamu.&quot;,&#10;        };&#10;    },&#10;&#10;    async loginOtp(data: LoginOtpDto) {&#10;        const buyer = await prisma.pembeli.findFirst({&#10;            where: { phone_number: data.phoneNumber }&#10;        });&#10;        if (!buyer) {&#10;            throw new Error(&quot;Nomor telepon tidak terdaftar sebagai pembeli.&quot;);&#10;        }&#10;        // Generate OTP&#10;        const otp = Math.floor(10000 + Math.random() * 90000).toString();&#10;        const expiredAt = new Date(Date.now() + OTP_EXPIRE_MINUTES * 60000);&#10;        await prisma.otp_verify.create({&#10;            data: {&#10;                phone: data.phoneNumber,&#10;                otp,&#10;                expiredAt,&#10;            }&#10;        });&#10;        // Simulasi kirim OTP via console&#10;        console.log(`OTP ${otp} dikirim ke ${data.phoneNumber}`);&#10;        return { message: &quot;OTP dikirim ke nomor kamu.&quot; };&#10;    },&#10;&#10;    async verifyOtp(data: VerifyOtpDto) {&#10;        const otpRecord = await prisma.otp_verify.findFirst({&#10;            where: {&#10;                phone: data.phoneNumber,&#10;                otp: data.otp,&#10;                expiredAt: { gte: new Date() }&#10;            }&#10;        });&#10;        if (!otpRecord) {&#10;            throw new Error(&quot;OTP salah atau sudah expired.&quot;);&#10;        }&#10;        const buyer = await prisma.pembeli.findFirst({&#10;            where: { phone_number: data.phoneNumber }&#10;        });&#10;        if (!buyer) {&#10;            throw new Error(&quot;Pembeli tidak ditemukan.&quot;);&#10;        }&#10;        // Generate JWT&#10;        const token = jwt.sign({ id: buyer.user_id, role: &quot;pembeli&quot; }, JWT_SECRET, { expiresIn: &quot;1d&quot; });&#10;        // Hapus OTP setelah sukses verifikasi&#10;        await prisma.otp_verify.delete({ where: { id: otpRecord.id } });&#10;        return { token };&#10;    },&#10;&#10;&#10;&#10;    async requestSellerOtp(data: RequestSellerOtpDto) {&#10;        // Generate OTP&#10;        const otp = Math.floor(10000 + Math.random() * 90000).toString();&#10;        const expiredAt = new Date(Date.now() + OTP_EXPIRE_MINUTES * 60000);&#10;        await prisma.otp_verify.create({&#10;            data: {&#10;                phone: data.phoneNumber,&#10;                otp,&#10;                expiredAt,&#10;            }&#10;        });&#10;        console.log(`OTP ${otp} dikirim ke ${data.phoneNumber}`);&#10;        return { message: &quot;OTP dikirim ke nomor kamu.&quot; };&#10;    },&#10;&#10;    async verifyOtpAndRegisterSeller(data: VerifyOtpAndRegisterSellerDto) {&#10;        // Cek OTP&#10;        const otpRecord = await prisma.otp_verify.findFirst({&#10;            where: {&#10;                phone: data.phoneNumber,&#10;                otp: data.otp,&#10;                expiredAt: { gte: new Date() }&#10;            }&#10;        });&#10;        if (!otpRecord) throw new Error(&quot;OTP salah atau sudah expired.&quot;);&#10;        // Cek email/phone sudah terdaftar&#10;        const existing = await prisma.penjual.findFirst({&#10;            where: {&#10;                OR: [&#10;                    { email: data.email },&#10;                    { phone_number: data.phoneNumber }&#10;                ]&#10;            }&#10;        });&#10;        if (existing) throw new Error(&quot;Nomor atau email sudah terdaftar sebagai penjual.&quot;);&#10;        // Hash password&#10;        const hashedPassword = await bcrypt.hash(data.password, 10);&#10;        // Simpan penjual&#10;        const seller = await prisma.penjual.create({&#10;            data: {&#10;                phone_number: data.phoneNumber,&#10;                email: data.email,&#10;                password: hashedPassword&#10;            }&#10;        });&#10;        // Hapus OTP&#10;        await prisma.otp_verify.delete({ where: { id: otpRecord.id } });&#10;        // Generate JWT&#10;        const token = jwt.sign({ sellerId: seller.mitra_id, role: &quot;penjual&quot; }, JWT_SECRET, { expiresIn: &quot;7d&quot; });&#10;        return { token };&#10;    },&#10;&#10;    async registerSellerStore(sellerId: number, data: RegisterSellerStoreDto) {&#10;        await prisma.penjual.update({&#10;            where: { mitra_id: sellerId },&#10;            data: {&#10;                jenis_usaha: data.businessType,&#10;                foto_ktp_pemilik: data.ownerKtpPhoto,&#10;                nama_toko: data.storeName,&#10;                alamat_toko: data.storeAddress,&#10;                foto_pemilik: data.ownerFacePhoto,&#10;                status_verifikasi: &quot;pending&quot;&#10;            }&#10;        });&#10;        return { message: &quot;Profil toko berhasil dikirim, menunggu verifikasi.&quot; };&#10;    },&#10;};&#10;" />
              <option name="updatedContent" value="import { PrismaClient } from &quot;@prisma/client&quot;;&#10;import { RegisterBuyerDto, LoginOtpDto, VerifyOtpDto, RequestSellerOtpDto, VerifyOtpAndRegisterSellerDto, RegisterSellerStoreDto } from &quot;./auth.schema&quot;;&#10;import jwt from &quot;jsonwebtoken&quot;;&#10;import bcrypt from &quot;bcryptjs&quot;;&#10;&#10;const prisma = new PrismaClient();&#10;const OTP_EXPIRE_MINUTES = 5;&#10;const JWT_SECRET = process.env.JWT_SECRET;&#10;if (!JWT_SECRET) {&#10;    throw new Error(&quot;JWT_SECRET tidak ditemukan di environment variable. Pastikan .env sudah benar!&quot;);&#10;}&#10;&#10;function generateToken(payload: object, expiresIn: string | number = &quot;1d&quot;) {&#10;    console.debug(&quot;[DEBUG generateToken] JWT_SECRET:&quot;, JWT_SECRET);&#10;    return jwt.sign(payload, JWT_SECRET, { expiresIn });&#10;}&#10;&#10;export const authService = {&#10;    async registerBuyer(data: RegisterBuyerDto) {&#10;        const existing = await prisma.pembeli.findFirst({&#10;            where: {&#10;                OR: [&#10;                    { phone_number: data.phoneNumber },&#10;                    { email: data.email }&#10;                ]&#10;            }&#10;        });&#10;&#10;&#10;        if (existing) {&#10;            throw new Error(&quot;Nomor atau email sudah terdaftar.&quot;);&#10;        }&#10;&#10;        // Simpan data pembeli baru&#10;        const newBuyer = await prisma.pembeli.create({&#10;            data: {&#10;                full_name: data.fullName,&#10;                phone_number: data.phoneNumber,&#10;                delivery_address: data.address,&#10;                birth_date: new Date(data.birthDate),&#10;                domicile: data.domicile,&#10;                faculty: data.faculty,&#10;                major: data.major,&#10;                email: data.email,&#10;                // password: ... (jika ada)&#10;            }&#10;        });&#10;        // Generate OTP&#10;        const otp = Math.floor(10000 + Math.random() * 90000).toString();&#10;        const expiredAt = new Date(Date.now() + OTP_EXPIRE_MINUTES * 60000);&#10;        await prisma.otp_verify.create({&#10;            data: {&#10;                phone: data.phoneNumber,&#10;                otp,&#10;                expiredAt,&#10;            }&#10;        });&#10;        // Simulasi kirim OTP via console&#10;        console.log(`OTP ${otp} dikirim ke ${data.phoneNumber}`);&#10;        return {&#10;            message: &quot;Register berhasil. OTP dikirim ke nomor kamu.&quot;,&#10;        };&#10;    },&#10;&#10;    async loginOtp(data: LoginOtpDto) {&#10;        const buyer = await prisma.pembeli.findFirst({&#10;            where: { phone_number: data.phoneNumber }&#10;        });&#10;        if (!buyer) {&#10;            throw new Error(&quot;Nomor telepon tidak terdaftar sebagai pembeli.&quot;);&#10;        }&#10;        // Generate OTP&#10;        const otp = Math.floor(10000 + Math.random() * 90000).toString();&#10;        const expiredAt = new Date(Date.now() + OTP_EXPIRE_MINUTES * 60000);&#10;        await prisma.otp_verify.create({&#10;            data: {&#10;                phone: data.phoneNumber,&#10;                otp,&#10;                expiredAt,&#10;            }&#10;        });&#10;        // Simulasi kirim OTP via console&#10;        console.log(`OTP ${otp} dikirim ke ${data.phoneNumber}`);&#10;        return { message: &quot;OTP dikirim ke nomor kamu.&quot; };&#10;    },&#10;&#10;    async verifyOtp(data: VerifyOtpDto) {&#10;        const otpRecord = await prisma.otp_verify.findFirst({&#10;            where: {&#10;                phone: data.phoneNumber,&#10;                otp: data.otp,&#10;                expiredAt: { gte: new Date() }&#10;            }&#10;        });&#10;        if (!otpRecord) {&#10;            throw new Error(&quot;OTP salah atau sudah expired.&quot;);&#10;        }&#10;        const buyer = await prisma.pembeli.findFirst({&#10;            where: { phone_number: data.phoneNumber }&#10;        });&#10;        if (!buyer) {&#10;            throw new Error(&quot;Pembeli tidak ditemukan.&quot;);&#10;        }&#10;        // Generate JWT&#10;        console.debug(&quot;[DEBUG verifyOtp] JWT_SECRET:&quot;, JWT_SECRET);&#10;        const token = generateToken({ id: buyer.user_id, role: &quot;pembeli&quot; }, &quot;1d&quot;);&#10;        // Hapus OTP setelah sukses verifikasi&#10;        await prisma.otp_verify.delete({ where: { id: otpRecord.id } });&#10;        return { token };&#10;    },&#10;&#10;&#10;&#10;    async requestSellerOtp(data: RequestSellerOtpDto) {&#10;        // Generate OTP&#10;        const otp = Math.floor(10000 + Math.random() * 90000).toString();&#10;        const expiredAt = new Date(Date.now() + OTP_EXPIRE_MINUTES * 60000);&#10;        await prisma.otp_verify.create({&#10;            data: {&#10;                phone: data.phoneNumber,&#10;                otp,&#10;                expiredAt,&#10;            }&#10;        });&#10;        console.log(`OTP ${otp} dikirim ke ${data.phoneNumber}`);&#10;        return { message: &quot;OTP dikirim ke nomor kamu.&quot; };&#10;    },&#10;&#10;    async verifyOtpAndRegisterSeller(data: VerifyOtpAndRegisterSellerDto) {&#10;        // Cek OTP&#10;        const otpRecord = await prisma.otp_verify.findFirst({&#10;            where: {&#10;                phone: data.phoneNumber,&#10;                otp: data.otp,&#10;                expiredAt: { gte: new Date() }&#10;            }&#10;        });&#10;        if (!otpRecord) throw new Error(&quot;OTP salah atau sudah expired.&quot;);&#10;        // Cek email/phone sudah terdaftar&#10;        const existing = await prisma.penjual.findFirst({&#10;            where: {&#10;                OR: [&#10;                    { email: data.email },&#10;                    { phone_number: data.phoneNumber }&#10;                ]&#10;            }&#10;        });&#10;        if (existing) throw new Error(&quot;Nomor atau email sudah terdaftar sebagai penjual.&quot;);&#10;        // Hash password&#10;        const hashedPassword = await bcrypt.hash(data.password, 10);&#10;        // Simpan penjual&#10;        const seller = await prisma.penjual.create({&#10;            data: {&#10;                phone_number: data.phoneNumber,&#10;                email: data.email,&#10;                password: hashedPassword&#10;            }&#10;        });&#10;        // Hapus OTP&#10;        await prisma.otp_verify.delete({ where: { id: otpRecord.id } });&#10;        // Generate JWT&#10;        console.debug(&quot;[DEBUG verifyOtpAndRegisterSeller] JWT_SECRET:&quot;, JWT_SECRET);&#10;        const token = generateToken({ sellerId: seller.mitra_id, role: &quot;penjual&quot; }, &quot;7d&quot;);&#10;        return { token };&#10;    },&#10;&#10;    async registerSellerStore(sellerId: number, data: RegisterSellerStoreDto) {&#10;        await prisma.penjual.update({&#10;            where: { mitra_id: sellerId },&#10;            data: {&#10;                jenis_usaha: data.businessType,&#10;                foto_ktp_pemilik: data.ownerKtpPhoto,&#10;                nama_toko: data.storeName,&#10;                alamat_toko: data.storeAddress,&#10;                foto_pemilik: data.ownerFacePhoto,&#10;                status_verifikasi: &quot;pending&quot;&#10;            }&#10;        });&#10;        return { message: &quot;Profil toko berhasil dikirim, menunggu verifikasi.&quot; };&#10;    },&#10;};" />
            </PendingDiffInfo>
          </value>
        </entry>
        <entry key="$PROJECT_DIR$/src/modules/auth/jwt.middleware.ts">
          <value>
            <PendingDiffInfo>
              <option name="filePath" value="$PROJECT_DIR$/src/modules/auth/jwt.middleware.ts" />
              <option name="originalContent" value="import { Request, Response, NextFunction } from &quot;express&quot;;&#10;import jwt from &quot;jsonwebtoken&quot;;&#10;&#10;const JWT_SECRET = process.env.JWT_SECRET || &quot;secret&quot;;&#10;&#10;export function jwtMiddleware(req: Request, res: Response, next: NextFunction) {&#10;    const authHeader = req.headers.authorization;&#10;    if (!authHeader || !authHeader.startsWith(&quot;Bearer &quot;)) {&#10;        return res.status(401).json({ message: &quot;Token tidak ditemukan&quot; });&#10;    }&#10;    const token = authHeader.split(&quot; &quot;)[1];&#10;    try {&#10;        const decoded = jwt.verify(token, JWT_SECRET);&#10;        (req as any).user = decoded;&#10;        next();&#10;    } catch (err) {&#10;        return res.status(401).json({ message: &quot;Token tidak valid&quot; });&#10;    }&#10;}&#10;&#10;" />
              <option name="updatedContent" value="import { Request, Response, NextFunction } from &quot;express&quot;;&#10;import jwt from &quot;jsonwebtoken&quot;;&#10;&#10;const JWT_SECRET = process.env.JWT_SECRET || &quot;secret&quot;;&#10;&#10;export function jwtMiddleware(req: Request, res: Response, next: NextFunction) {&#10;    console.log(&quot;[DEBUG jwt.middleware] JWT_SECRET:&quot;, JWT_SECRET);&#10;    const authHeader = req.headers.authorization;&#10;    console.log(&quot;[DEBUG jwt.middleware] Authorization Header:&quot;, authHeader);&#10;    if (!authHeader || !authHeader.startsWith(&quot;Bearer &quot;)) {&#10;        return res.status(401).json({ message: &quot;Token tidak ditemukan&quot; });&#10;    }&#10;    const token = authHeader.split(&quot; &quot;)[1];&#10;    try {&#10;        const decoded = jwt.verify(token, JWT_SECRET);&#10;        console.log(&quot;[DEBUG jwt.middleware] Decoded Token:&quot;, decoded);&#10;        (req as any).user = decoded;&#10;        next();&#10;    } catch (err) {&#10;        console.log(&quot;[DEBUG jwt.middleware] JWT Error:&quot;, err);&#10;        return res.status(401).json({ message: &quot;Token tidak valid&quot; });&#10;    }&#10;}" />
            </PendingDiffInfo>
          </value>
        </entry>
      </map>
    </option>
  </component>
</project>